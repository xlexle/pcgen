
import { asWatts } from 'pcgen-front/helpers/as-watts';
import { module, test } from 'qunit';

module('Unit | Helper | as watts');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = asWatts([42]);
  assert.ok(result);
});

